namespace SmartWard.Infrastructure.Events
{
    public enum EventType
    {
        ActivityEvents,
        DeviceEvents,
        ComEvents,
        FileEvents,
        UserEvent,
        StatusEvent
    }
}
